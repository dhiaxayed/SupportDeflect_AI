# SupportDeflect AI frontend — environment configuration (example)
# Copy to .env and adjust as needed.
# Base URL of the FastAPI backend (including the /api/v1 prefix).
VITE_API_URL=http://127.0.0.1:8000/api/v1
